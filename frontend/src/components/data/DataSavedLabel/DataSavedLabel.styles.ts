import { alpha, styled } from "@mui/material";

export const UpdateLabel = styled("div")(({ }) => ({
  textAlign: "center",
  padding: "5px 10px",
  borderRadius: 5,
  background: alpha("#fff", 0.3)
}));
