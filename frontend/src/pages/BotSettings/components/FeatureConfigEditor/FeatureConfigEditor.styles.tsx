import { styled, Typography } from "@mui/material";

export const ProofInputEntryTitle = styled(Typography)({
  fontSize: "14px",
  opacity: 0.5
});

export const SectionTitle = styled(Typography)({

});

export const SectionIntro = styled(Typography)({

});

export const DescriptionLabel = styled(Typography)({
  fontSize: 11,
  opacity: 0.7
});

export const PropertyNameLabel = styled(Typography)({
  textTransform: "capitalize"
});