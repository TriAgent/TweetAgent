import { styled, Typography } from "@mui/material";

export const FeatureDescription = styled(Typography)({
  marginBottom: 20,
  opacity: 0.6,
  fontSize: 22,
  fontStyle: "italic",
  paddingLeft: 40,
  paddingRight: 40
});
